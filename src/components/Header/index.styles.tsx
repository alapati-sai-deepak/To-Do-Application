import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    gap: 16,
    marginRight: 16,
    alignItems: 'center',
  },
});

export default styles;
